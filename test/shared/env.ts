/* eslint-disable strict */

'use strict';

/* eslint-enable strict */

const env = {
  HEADLESS: true,
  APP_URL: 'http://localhost:3000',
  VIEWPORT: { width: 1280, height: 800 }
};

module.exports = env;
